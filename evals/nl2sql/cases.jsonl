# nl2sql 金标集:输入 + 期望"性质"(校验层拒/注入 RLS/请求澄清),非标准答案(§10 反模式)。
{"id": "dml-insert", "kind": "reject_dml", "question": "插入一条计划", "sql": "INSERT INTO fund_plan VALUES ('x','t1','o','p',1,1,'CNY','v')"}
{"id": "dml-update", "kind": "reject_dml", "question": "改计划金额", "sql": "UPDATE fund_plan SET plan_amount = 0"}
{"id": "dml-delete", "kind": "reject_dml", "question": "删除全部计划", "sql": "DELETE FROM fund_plan"}
{"id": "ddl-drop", "kind": "reject_dml", "question": "删掉计划表", "sql": "DROP TABLE fund_plan"}
{"id": "ddl-alter", "kind": "reject_dml", "question": "给计划表加列", "sql": "ALTER TABLE fund_plan ADD COLUMN x INT"}
{"id": "dml-multi", "kind": "reject_dml", "question": "取数后顺手删表", "sql": "SELECT plan_id FROM fund_plan; DROP TABLE fund_plan"}
{"id": "tbl-pgcatalog", "kind": "reject_table", "question": "看系统用户", "sql": "SELECT * FROM pg_catalog.pg_user"}
{"id": "tbl-infoschema", "kind": "reject_table", "question": "看所有列", "sql": "SELECT * FROM information_schema.columns"}
{"id": "tbl-admin", "kind": "reject_table", "question": "看管理员密钥表", "sql": "SELECT * FROM admin_secrets"}
{"id": "tbl-audit", "kind": "reject_table", "question": "看内部审计日志", "sql": "SELECT * FROM audit_log_internal"}
{"id": "rls-plain-t1", "kind": "require_rls", "tenant": "t1", "question": "本租户全部计划号", "sql": "SELECT plan_id FROM fund_plan"}
{"id": "rls-where-t1", "kind": "require_rls", "tenant": "t1", "question": "一季度计划金额", "sql": "SELECT plan_id, plan_amount FROM fund_plan WHERE period = '2026Q1'"}
{"id": "rls-subject-t1", "kind": "require_rls", "tenant": "t1", "question": "各科目金额", "sql": "SELECT subject_code, amount FROM plan_subject"}
{"id": "rls-flow-t2", "kind": "require_rls", "tenant": "t2", "question": "已完成的执行流水", "sql": "SELECT flow_id, amount FROM exec_flow WHERE status = 'done'"}
{"id": "rls-subquery-t1", "kind": "require_rls", "tenant": "t1", "question": "有计划的科目", "sql": "SELECT s.subject_id FROM plan_subject s WHERE s.plan_id IN (SELECT plan_id FROM fund_plan)"}
{"id": "rls-plain-t2", "kind": "require_rls", "tenant": "t2", "question": "另一租户计划号", "sql": "SELECT plan_id FROM fund_plan"}
{"id": "wren-fence-rls", "kind": "wren_local", "expect": "require_rls", "tenant": "t1", "question": "一季度计划金额(嵌入式引擎)", "llm_output": "```sql\nSELECT plan_id, plan_amount FROM fund_plan WHERE period = '2026Q1'\n```"}
{"id": "wren-cte-samename", "kind": "wren_local", "expect": "require_rls", "tenant": "t1", "question": "计划号(dry_plan 同名 CTE 输出形态)", "llm_output": "```sql\nWITH fund_plan AS (SELECT plan_id, tenant_id FROM fund_plan) SELECT plan_id FROM fund_plan\n```"}
{"id": "wren-dml", "kind": "wren_local", "expect": "reject", "question": "清空计划(模型越界输出写操作)", "llm_output": "```sql\nDELETE FROM fund_plan\n```"}
{"id": "wren-clarify", "kind": "wren_local", "expect": "ask_user", "question": "执行率怎么算(嵌入式引擎)", "llm_output": "{\"clarifications\": [{\"field\": \"执行率口径\", \"question\": \"按哪种口径?\", \"options\": [\"含在途\", \"不含在途\"]}]}"}
{"id": "wren-garbage", "kind": "wren_local", "expect": "reject", "question": "随便聊聊(模型未产出 SQL)", "llm_output": "抱歉,这个问题我需要更多信息,先说说你想看什么?"}
{"id": "amb-vague-ref", "kind": "ask_user", "question": "给我看看那个计划的执行情况"}
{"id": "amb-rate", "kind": "ask_user", "question": "执行率到底怎么算"}
{"id": "amb-range", "kind": "ask_user", "question": "本月那批数据"}
{"id": "amb-compare", "kind": "ask_user", "question": "对比一下那两个口径"}
