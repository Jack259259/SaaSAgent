# 资金计划 Agent —— 本地基础设施(阶段 0:全部默认注释停用)。
# 启用方式:取消对应服务的注释,cp .env.example .env 并填好变量,然后 docker compose up -d。
# 说明:
#   - 本阶段 make dev 为未实现桩(见 PROGRESS.md),本文件暂不会被任何命令使用;
#   - Langfuse v3 自托管需 ClickHouse / Redis / MinIO,此处先按 v2 单容器写最小定义,
#     观测接入阶段(阶段 10)再评估升级;
#   - 下方口令仅为本地开发占位,启用阶段应改为从 .env 注入(CLAUDE.md §6:严禁硬编码密钥)。

name: zijin-plan-agent

# services:
#   postgres:
#     image: postgres:16
#     environment:
#       POSTGRES_USER: agent
#       POSTGRES_PASSWORD: agent-local-only
#       POSTGRES_DB: agent
#     ports:
#       - "5432:5432"
#     volumes:
#       - pgdata:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U agent"]
#       interval: 5s
#       timeout: 3s
#       retries: 10
#
#   langfuse:
#     image: langfuse/langfuse:2
#     depends_on:
#       postgres:
#         condition: service_healthy
#     environment:
#       DATABASE_URL: postgresql://agent:agent-local-only@postgres:5432/agent
#       NEXTAUTH_SECRET: change-me-local-only
#       NEXTAUTH_URL: http://localhost:3000
#       SALT: change-me-local-only
#     ports:
#       - "3000:3000"
#
# volumes:
#   pgdata:
