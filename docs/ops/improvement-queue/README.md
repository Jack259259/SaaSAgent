# docs/ops/improvement-queue —— 改进建议队列(运行产物)

reflection-worker 事后反思产出的改进建议(检索 miss / 语义层口径 / 文档 / SOP 缺口)在此落
`<id>.json`,派发给对应资产负责人(§8.2,资产持续运营输入源)。
**JSON 为运行产物,不入库**(`.gitignore: docs/ops/**/*.json`)。
