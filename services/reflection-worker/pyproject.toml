[project]
name = "reflection-worker"
version = "0.1.0"
description = "事后反思:消费任务终态 → 情景记忆 + 经验条目(评分门槛)+ 改进工单(方案 §8)"
requires-python = ">=3.12"
dependencies = [
    "contracts",
    "memory-svc",
    "pydantic>=2.7",
]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

[tool.uv.sources]
contracts = { workspace = true }
memory-svc = { workspace = true }
