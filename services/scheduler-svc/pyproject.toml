[project]
name = "scheduler-svc"
version = "0.1.0"
description = "定时订阅 + 模板化通知 + 转人工(助手域副作用,方案 §5.5)"
requires-python = ">=3.12"
dependencies = [
    "contracts",
    "pydantic>=2.7",
    "httpx>=0.27",
]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

[tool.uv.sources]
contracts = { workspace = true }
