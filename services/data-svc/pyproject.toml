[project]
name = "data-svc"
version = "0.1.0"
description = "WrenAI 代理 + SQL 校验层 + RLS 注入 + 只读执行(CLAUDE.md §3 / 方案 §11.1)"
requires-python = ">=3.12"
dependencies = [
    "common",
    "fastapi>=0.115",
    "pydantic>=2.7",
    "structlog>=24.1",
]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"

[tool.uv.sources]
common = { workspace = true }
