[project]
name = "common"
version = "0.1.0"
description = "共享底座:user_ctx 透传、审计事件、错误枚举(CLAUDE.md §3 packages/common)"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.7",
    "structlog>=24.1",
]

[build-system]
requires = ["uv_build>=0.11,<0.12"]
build-backend = "uv_build"
